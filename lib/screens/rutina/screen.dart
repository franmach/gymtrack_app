import 'package:flutter/material.dart';
class RutinaScreen extends StatelessWidget {
  const RutinaScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('Rutina')));
}